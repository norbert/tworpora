from setuptools import setup


setup(name='tworpora',
      version='0.0.1',
      packages=['tworpora'],
      install_requires=['beautifulsoup4', 'python-twitter>=1.1',
                        'joblib>=0.8.0a3'])
